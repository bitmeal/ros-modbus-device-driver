{
    "dockerFile": "Dockerfile",
    "extensions": ["ms-iot.vscode-ros"],
    "workspaceFolder": "/catkin_ws",
    "workspaceMount": "source=${localWorkspaceFolder},target=/catkin_ws/src/ros_modbus_device,type=bind",
    "postStartCommand": "/opt/ros/$ROS_DISTRO/setup.bash && /catkin_ws/devel/setup.bash && apt-get update && rosdep update && rosdep install --from-paths src --ignore-src --rosdistro $ROS_DISTRO -y"
}